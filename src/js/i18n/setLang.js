/**
 * Created by hepen on 5/27/2017.
 */
import {IntlProvider, addLocaleData} from 'react-intl';

module.exports = {
  setLocaleResource: function(fileName) {
    let currentLang = navigator.language || navigator.browserLanguage;
    
  }
}
